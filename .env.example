# Kimi (Moonshot AI) API 配置
# 复制此文件为 .env 并填入你的 API Key

# Moonshot API Key（必需）
# 获取方式：https://platform.moonshot.cn/
MOONSHOT_API_KEY=your_moonshot_api_key_here

# API 基础 URL（可选，默认: https://api.moonshot.cn/v1）
# 通常不需要修改，除非使用自定义的 API 端点
# MOONSHOT_BASE_URL=https://api.moonshot.cn/v1

# 模型选择（可选，默认: kimi-k2.5）
# 支持的模型：
# - kimi-k2.5 (推荐): 最全能模型，256k 上下文，最强的代码和 Agent 能力，默认启用思考
# - kimi-k2-0905-preview: K2 预览版，256k 上下文，强 Agentic Coding 能力
# - kimi-k2-0711-preview: K2 早期版本，128k 上下文
# - kimi-k2-turbo-preview: K2 高速版，速度可达 100 tokens/s
# - kimi-k2-thinking: 思考模型，256k 上下文，深度推理
# - kimi-k2-thinking-turbo: 思考模型高速版
# - kimi-latest: 始终指向最新版本
MOONSHOT_MODEL=kimi-k2.5

# 是否启用深度思考模式（可选，默认: false）
# 仅对非 thinking 模型有效，thinking 模型和 kimi-k2.5 默认开启
# MOONSHOT_ENABLE_THINKING=true

# 是否强制尝试新解法（可选，默认: false）
# 当题目文件已存在时：
# true: 即使文件存在也尝试提供新的解法思路（会覆盖原有文件）
# false: 如果文件已存在则跳过（默认行为）
# AI_SOLVER_FORCE_NEW_SOLUTION=false
